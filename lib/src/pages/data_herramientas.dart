import 'package:flutter/material.dart';

class DataHerramientas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Holaaaaaa'),
    );
  }
}
